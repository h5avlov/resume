
/* function smoothScrollInit( linksContainer, scrollTarget ) {
	linksContainer.on( "click", function( e ) {
		eventHandler( {
			target: $( e.target ), 
			element: ".link", 
			handlerArgs: {
				scrollTarget: scrollTarget
			}, 
			handler: function( handlerArgs ) {
				$.smoothScroll( {
					scrollTarget: handlerArgs.scrollTarget
				} );
			}
		} );
		
	} );
} */