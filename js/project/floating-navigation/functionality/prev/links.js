function linksHandlersInit( navigationUtilities ) {
	/* const floatingNav = $( ".floating-nav" ), 
		links = floatingNav.find( ".nav-links .link" ), 
		linkLabels = floatingNav.find( ".label-wrapper .link-label" ); */
		
	return {
		/* mouseOver: function( link ) {
			const linkPos = navigationUtilities.getTargetSectionPos( link ), 
				label = linkLabels.eq( linkPos );
			labelInteraction.on( label );
		},  */
		/* mouseOut: function() {
			if ( currentLabel === null ) {
				return; 
			}
			labelInteraction.off( currentLabel ); 
		},  */
		/* click: function( link ) {
			navigationUtilities.clickHandler( link ); 
		} */
	}; 
	
	
}














